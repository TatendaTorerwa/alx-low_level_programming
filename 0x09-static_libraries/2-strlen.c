#include "main.h"
#include <string.h>

/**
*_strlen - calculate the length
*
*@s: pointer
*
*Description: "calculate the length"
*
*Return: always 0 (success)
*/

int _strlen(char *s)
{
	return (strlen(s));
}
