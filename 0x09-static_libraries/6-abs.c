#include "main.h"
#include <stdlib.h>

/**
*_abs - returns a positive value
*
*@n: integer arg
*
*Description: "returns a positive value"
*
*Return: always 0 (success)
*/

int _abs(int n)
{
	return (abs(n));
}
