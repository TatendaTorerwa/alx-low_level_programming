#include "main.h"
#include <ctype.h>

/**
*_isdigit - check the character if it is a digit
*
*@c: integer arg
*
*Description: "check the character if it is a digit"
*
*Return: always 0 (success)
*/

int _isdigit(int c)
{
	return (isdigit(c));
}
