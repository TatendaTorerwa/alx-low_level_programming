#include "main.h"
#include <string.h>

/**
*_strcat - to concatenates a string to another
*
*@dest: pointer
*
*@src: pointer
*
*Description: "to concatenates a string to another"
*
*Return: pointer
*/

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
