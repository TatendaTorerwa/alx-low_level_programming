#include "main.h"
#include <stdlib.h>

/**
*_atoi - it converts a string to an integer
*
*@s: pointer
*
*Description: "it converts a string to an integer"
*
*Return: always 0 (success)
*/

int _atoi(char *s)

{
	return (atoi(s));
}
